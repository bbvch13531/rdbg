from package/readlog.py import 
def createRdbg(blogType, postId, title):
    if blogType == 'agile':
        url = "http://agile.egloos.com/"
        url += postId

def main():
    createRdbg(agile, 
if __name__ == '__main':
    main()
